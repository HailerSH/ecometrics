############################
# Django
############################
# Replace in your real .env
DJANGO_SECRET_KEY=change_me_in_production

# 1 for development, 0 for production
DJANGO_DEBUG=1

# Comma-separated list of allowed hosts
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

############################
# Database (PostgreSQL)
############################
DB_NAME=ecometrics
DB_USER=ecometrics
DB_PASSWORD=ecometrics
DB_HOST=db
DB_PORT=5432

############################
# Application
############################
# Public API base (used by Angular build if needed)
API_BASE_URL=http://localhost:8000/api

# Timezone example
APP_TIMEZONE=UTC
